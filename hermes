#!/usr/bin/env bash
# Repo-root dev launcher for PapyloNation Agent.
# Re-exec the bundled venv so deps (yaml, etc.) resolve the same way as the
# installed `papylonation`/`hermes` command.
DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
exec "$DIR/venv/bin/hermes" "$@"
